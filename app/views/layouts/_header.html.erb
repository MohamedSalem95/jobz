<nav class="navbar navbar-expand-lg navbar-dark bg-success mb-2">
	<div class="container">
  <a class="navbar-brand" href="/">JobZ</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      
    </ul>

    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
    	<% if user_signed_in? %>

    	

      <li class="nav-item">
        <div class="dropdown">
  <button class="btn btn-info dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <% if !current_user.image.profile.url.nil? %>
      <%= image_tag current_user.image.profile.url, width: 27, class: 'img-thumbnail' %>

    <% else %>
      <%= image_tag 'user_avatar.jpg', width: 27, class: 'rounded-circle' %>
    <% end %>

    <%= current_user.name.capitalize %>
  </button>
  <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
    <%= link_to 'Profile', profile_path(current_user), class: 'dropdown-item' %>

    <%= link_to 'Logout', destroy_user_session_path, class: 'dropdown-item', method: :delete %>
  </div>
</div>
      </li>

    	<% else %>

    	<li class="nav-item mr-1">
        <%= link_to 'Login', new_user_session_path, class: 'btn btn-primary' %>
      </li>

      <li class="nav-item">
        <%= link_to 'Register', new_user_registration_path, class: 'btn btn-secondary' %>
      </li>


    	<% end %>
    </ul>
  </div>
</div>
</nav>